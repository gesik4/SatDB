<h1><%= @compression.name %></h1>
<p><span class="badge">Satellites count: <%= @compression.satellites.count %></span></p>
<%= @compression.description %>
<br>
<br>
<%= render @compression.satellites%>
<br>
<br>
<% if session[:mentor_id] %>
    <%= link_to 'delete', @compression, method: :delete, class: 'btn btn-danger',
                data: { confirm: "You sure?" } %>
<% end %>